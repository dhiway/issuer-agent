/*!
  JSON ABC | License: MIT.
*/
declare const _default: {
    sort: typeof sort;
    sortObj: typeof sortObj;
    cleanJSON: typeof cleanJSON;
};
export default _default;
/**
 * Sort the JSON (clean, parse, sort, stringify).
 * @param noArray Sort or don't sort arrays
 */
export declare function sortObj<T extends object>(un: T, noArray?: boolean): T;
/** Remove trailing commas */
export declare function cleanJSON(input: string): string;
/**
 * Sort the JSON (clean, parse, sort, stringify).
 * @param noArray Sort or don't sort arrays
 */
export declare function sort(inputStr: string, noArray?: boolean): string;
